package com.jocmp.basil.common

import org.junit.Test


class DecodeHTMLTest {
    @Test
    fun `it decodes from HTML escaping`() {
//        val result = decodeHTML(text = "R&amp;A")
//
//        assertEquals(expected = "R&A", actual = result)
    }
}
