all:
SELECT *
FROM folders;

upsert:
INSERT INTO folders(
    name,
    expanded
)
VALUES (
    :name,
    :expanded
)
ON CONFLICT(name) DO UPDATE SET expanded = excluded.expanded;

find:
SELECT expanded
FROM folders
WHERE name = :name;
